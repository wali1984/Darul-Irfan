{
  "name": "darul-irfan-platform",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply darul-irfan --local",
    "db:migrate:remote": "wrangler d1 migrations apply darul-irfan --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260723.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10",
    "wrangler": "^4.114.0"
  }
}
