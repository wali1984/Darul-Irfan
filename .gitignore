# Xcode
*.xcodeproj/
!*.xcodeproj/project.pbxproj.keep
*.xcworkspace/
xcuserdata/
DerivedData/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# XcodeGen output is regenerated from project.yml on macOS
# (project.yml IS tracked; the generated .xcodeproj is not)

# Content ingest tool
Tools/ContentIngest/.venv/
Tools/ContentIngest/output/
Tools/ContentIngest/__pycache__/
**/__pycache__/
*.pyc

# Quran / tafsir OCR source material — large PDF scans + raw OCR JSON.
# NEVER commit: the verified text is imported into
# DarulIrfanApp/Resources/SeedData/*.json instead. Also keeps the machine-OCR
# proofreading pack (sacred text) out of the repo.
/Akram-u-Tarajum/
/Asrar-ut-Tanzil/
/Asrar-at-Tanzil — English tafsir/
/OCR/
Tools/OCR/

# Cloudflare Worker
Server/node_modules/
Server/.wrangler/
Server/.dev.vars
Server/coverage/

# OS
.DS_Store
Thumbs.db

# Secrets
.env
*.local.json
codemagic_private_key
codemagic_private_key.pub
# Local credentials handed to the agent — NEVER commit
cloudflare.md
*.token
*.secret
*.p8
AuthKey_*.p8
c:\Users\WajidAli\Downloads\ApiKey_QGS1YOMNXGRW.p8

# Hadith corpus staging (84 MB of per-book packs). Public domain
# (Unlicense) but too large for git; the seed ships hadith_books.json and
# the packs are bundled/downloaded at build time.
HadithData/
Asrar u Tanzil - Eng Urdu/
akram-ut-tafaseer-raw-v1/
